#version 410 core
in vec3 vnormal;
in vec3 vposition;
in vec3 vcolor;
in vec2 vtexCoord;

uniform sampler2D screenTexture;

out vec4 color;

void main()
{

    color = vec4(texture(screenTexture, vtexCoord).xyz,1.0);
}
